# ─────────────────────────────────────────────────────────────────
# datalib:modules/nbt/api/count
# Returns the element count of a list in storage.
#
# INPUT (storage datalib:input):
# storage → storage namespace
# path → list path
#
# OUTPUT: datalib:output result (int)
# ─────────────────────────────────────────────────────────────────

function datalib:modules/nbt/internal/count_exec with storage datalib:input {}
